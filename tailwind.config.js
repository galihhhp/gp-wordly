/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./index.html", "./src/**/*.{vue,js,ts,jsx,tsx}"], // Ensure all relevant files are included
  theme: {
    extend: {}, // Extend the default theme if needed
  },
  plugins: [], // Add plugins if required
};
